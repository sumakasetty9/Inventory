# Copy this file to .env and replace placeholders with your values.

# Database URL
# PostgreSQL (e.g. Supabase): postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# SQLite (local):             sqlite:///./inventory.db
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Low-stock warning: products with quantity <= this are considered low stock
LOW_STOCK_THRESHOLD=10
